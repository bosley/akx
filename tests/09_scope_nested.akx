(let x 10)
(io/putf "outer x = %d\n" x)

(let outer (lambda []
  (let x 20)
  x))

(let result (outer))
(io/putf "inner x (from lambda) = %d\n" result)
(io/putf "outer x (after lambda) = %d\n" x)

(assert/eq result 20)
(assert/eq x 10)
(io/putf "PASS: scope shadowing works\n")
