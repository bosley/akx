(io/putf "=== Forms Basic Tests ===\n")

(io/putf "Test 1: Define primitive form\n")
(form/define :Point :int)
(io/putf "  PASS: Primitive form defined\n")

(io/putf "Test 2: Match integer to int form\n")
(let x 42)
(if (form/matches x :Point)
  (io/putf "  PASS: Integer matches int form\n")
  (io/putf "  FAIL: Integer should match int form\n"))

(io/putf "Test 3: Reject real for int form\n")
(let y 3.14)
(if (form/matches y :Point)
  (io/putf "  FAIL: Real should not match int form\n")
  (io/putf "  PASS: Real rejected for int form\n"))

(io/putf "Test 4: Reject string for int form\n")
(let s "hello")
(if (form/matches s :Point)
  (io/putf "  FAIL: String should not match int form\n")
  (io/putf "  PASS: String rejected for int form\n"))

(io/putf "Test 5: Define real form\n")
(form/define :Temperature :real)
(io/putf "  PASS: Real form defined\n")

(io/putf "Test 6: Match real to real form\n")
(let temp 98.6)
(if (form/matches temp :Temperature)
  (io/putf "  PASS: Real matches real form\n")
  (io/putf "  FAIL: Real should match real form\n"))

(io/putf "Test 7: Reject integer for real form\n")
(let i 100)
(if (form/matches i :Temperature)
  (io/putf "  FAIL: Integer should not match real form\n")
  (io/putf "  PASS: Integer rejected for real form\n"))

(io/putf "Test 8: Define string form\n")
(form/define :Name :string)
(io/putf "  PASS: String form defined\n")

(io/putf "Test 9: Match string to string form\n")
(let name "Alice")
(if (form/matches name :Name)
  (io/putf "  PASS: String matches string form\n")
  (io/putf "  FAIL: String should match string form\n"))

(io/putf "Test 10: Reject integer for string form\n")
(let num 123)
(if (form/matches num :Name)
  (io/putf "  FAIL: Integer should not match string form\n")
  (io/putf "  PASS: Integer rejected for string form\n"))

(io/putf "Test 11: Define byte form\n")
(form/define :Byte :byte)
(io/putf "  PASS: Byte form defined\n")

(io/putf "Test 12: Define compound form\n")
(form/define :Coordinate { :int :int })
(io/putf "  PASS: Compound form defined\n")

(io/putf "Test 13: Define named form alias\n")
(form/define :Distance :int)
(io/putf "  PASS: Named form alias defined\n")

(io/putf "\n=== All Basic Tests Complete ===\n")
