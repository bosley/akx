(io/putf "Testing if builtin with integer 1 (true):\n")
(let result1 (if 1 "then-branch" "else-branch"))
(io/putf "  Result: %s\n" result1)

(io/putf "Testing if builtin with integer 0 (false):\n")
(let result2 (if 0 "then-branch" "else-branch"))
(io/putf "  Result: %s\n" result2)

(io/putf "Testing if builtin with integer 2 (false):\n")
(let result3 (if 2 "then-branch" "else-branch"))
(io/putf "  Result: %s\n" result3)

(io/putf "Testing if builtin with no else branch (false condition):\n")
(let result4 (if 0 "then-branch"))
(io/putf "  Result: %v\n" result4)

(io/putf "Testing if builtin with no else branch (true condition):\n")
(let result5 (if 1 "then-branch"))
(io/putf "  Result: %s\n" result5)

(io/putf "Testing if builtin with expression evaluation:\n")
(let x 1)
(let result6 (if x "x is 1" "x is not 1"))
(io/putf "  Result: %s\n" result6)

(io/putf "All if tests completed\n")

