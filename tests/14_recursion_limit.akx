(cjit-load-builtin println :root "tests/builtins/println.c")

(println "Testing recursion limit protection...")

(let infinite-loop (lambda []
  (infinite-loop)))

(infinite-loop)
(println "This should never print")
