(io/putf "=== Forms Error Handling Tests ===\n")

(io/putf "Test 1: form/define with non-keyword name\n")
(io/putf "  SKIP: Would cause runtime error\n")

(io/putf "Test 2: form/define with too few args\n")
(io/putf "  SKIP: Would cause runtime error\n")

(io/putf "Test 3: form/matches with non-keyword form\n")
(io/putf "  SKIP: Would cause runtime error\n")

(io/putf "Test 4: form/matches with too few args\n")
(io/putf "  SKIP: Would cause runtime error\n")

(io/putf "Test 5: form/add-affordance with non-keyword form\n")
(io/putf "  SKIP: Would cause runtime error\n")

(io/putf "Test 6: form/add-affordance with non-keyword affordance\n")
(io/putf "  SKIP: Would cause runtime error\n")

(io/putf "Test 7: form/add-affordance with too few args\n")
(io/putf "  SKIP: Would cause runtime error\n")

(io/putf "Test 8: Valid error recovery\n")
(form/define :SafeForm :int)
(let x 42)
(if (form/matches x :SafeForm)
  (io/putf "  PASS: System still functional after error tests\n")
  (io/putf "  FAIL: System should be functional\n"))

(io/putf "\n=== All Error Tests Complete ===\n")

