(let make-identity (lambda []
  (lambda [y] y)))

(let my-identity (make-identity))
(let result (my-identity 100))

(io/putf "make-identity()() returned: %d\n" result)
(assert-eq result 100)
(io/putf "PASS: higher-order lambdas work\n")
